import { observer } from "mobx-react-lite";
import { ReactElement } from "react";
import "./index.css";

export const DealGeneralSeller = observer((): ReactElement => {
    return <div className='grid deal-general-seller row-gap-2'></div>;
});
