import { observer } from "mobx-react-lite";
import { ReactElement } from "react";
import "./index.css";

export const DealRetailFinances = observer((): ReactElement => {
    return <div className='grid deal-retail-finances row-gap-2'></div>;
});
