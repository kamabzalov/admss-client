import { observer } from "mobx-react-lite";
import { ReactElement } from "react";
import "./index.css";

export const DealRetailProducts = observer((): ReactElement => {
    return <div className='grid deal-retail-products row-gap-2'></div>;
});
