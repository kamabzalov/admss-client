import { observer } from "mobx-react-lite";
import { ReactElement } from "react";
import "./index.css";

export const DealRetailContract = observer((): ReactElement => {
    return <div className='grid deal-retail-contract row-gap-2'></div>;
});
