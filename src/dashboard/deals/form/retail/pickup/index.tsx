import { observer } from "mobx-react-lite";
import { ReactElement } from "react";
import "./index.css";

export const DealRetailPickup = observer((): ReactElement => {
    return <div className='grid deal-retail-pickup row-gap-2'></div>;
});
