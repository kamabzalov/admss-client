import { ReactElement } from "react";

import { observer } from "mobx-react-lite";

export const AccountBalanceAdjustment = observer((): ReactElement => {
    return <div className='quick-pay'>Balance</div>;
});
