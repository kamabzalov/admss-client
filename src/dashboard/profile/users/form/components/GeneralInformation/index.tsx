import { ReactElement } from "react";
import { observer } from "mobx-react-lite";

export const GeneralInformation = observer((): ReactElement => {
    return <div className='user-form general-information'></div>;
});
