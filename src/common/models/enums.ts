/* eslint-disable no-unused-vars */

export enum MediaType {
    mtUnknown,
    mtPhoto,
    mtPhotoPreview,
    mtVideo,
    mtVideoPreview,
    mtAudio,
    mtPDFDocument,
    mtJSONDocument,
    mtBinaryData,
    mtDocument,
}

export enum ContentType {
    ctGeneral,
    ctInterior,
    ctExterior,
    ctDocument,
}
