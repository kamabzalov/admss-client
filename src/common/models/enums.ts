export enum MediaType {
    mtUnknown,
    mtPhoto,
    mtPhotoPreview,
    mtVideo,
    mtVideoPreview,
    mtAudio,
    mtPDFDocument,
    mtJSONDocument,
    mtBinaryData,
    mtDocument,
    mtLink,
}

export enum ContentType {
    ctGeneral,
    ctInterior,
    ctExterior,
    ctDocument,
}
