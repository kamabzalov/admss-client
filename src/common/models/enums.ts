export enum MediaType {
    mtUnknown,
    mtPhoto,
    mtPhotoPreview,
    mtVideo,
    mtVideoPreview,
    mtAudio,
    mtPDFDocument,
    mtJSONDocument,
    mtBinaryData,
    mtDocument,
    mtLink,
}

export enum ContentType {
    ctGeneral,
    ctInterior,
    ctExterior,
    ctDocument,
}

export enum MEDIA_EXTENSION {
    JPEG = "jpeg",
    JPG = "jpg",
    PNG = "png",
    PDF = "pdf",
    TIFF = "tiff",
}
